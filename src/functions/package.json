{
  "name": "watchtime-functions",
  "private": true,
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "scripts": {
    "clean": "rimraf lib",
    "build": "tsc -p tsconfig.json",
    "predeploy": "npm run build"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.1",
    "@genkit-ai/core": "^0.8.0",
    "@genkit-ai/firebase": "^0.8.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "rimraf": "^5.0.5"
  }
}
